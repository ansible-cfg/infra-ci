---
- hosts: all
  vars_prompt:
    - name: "root_new_password"
      prompt: "Root new password"
      #private: yes
 
  roles:
    - {role: user_declaration, usernames: ["arnauld"]}
    - ssh_disable_root
 